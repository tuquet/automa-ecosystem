---
"vscode-automa": minor
---

Refactored Welcome panel to use native VS Code UI components, completely removing the heavy `webview-ui` Vue bundle dependency. Extension VSIX size is significantly reduced and startup is faster.
