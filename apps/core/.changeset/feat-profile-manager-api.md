---
"automa-core": minor
---

Added `POST /api/profiles/{id}/launch` and `POST /api/profiles/{id}/kill` endpoints for remote profile management.
Implemented HashMap PID registry for tracking and safely terminating browser instances.
