name: Release Automa Core (Ecosystem)

on:
  push:
    tags:
      - 'core-v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. core-v0.1.0)'
        required: false
        type: string

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: automa-core
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary_name: automa-core.exe
            asset_name: automa-core-windows-x86_64.zip
          # Tạm thời comment out theo yêu cầu (chỉ build Windows)
          # - os: ubuntu-latest
          #   target: x86_64-unknown-linux-gnu
          #   binary_name: automa-core
          #   asset_name: automa-core-linux-x86_64.tar.gz
          # - os: macos-latest
          #   target: aarch64-apple-darwin
          #   binary_name: automa-core
          #   asset_name: automa-core-macos-arm64.tar.gz
          # - os: macos-latest
          #   target: x86_64-apple-darwin
          #   binary_name: automa-core
          #   asset_name: automa-core-macos-x86_64.tar.gz

    steps:
      - name: Checkout repository & submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo dependencies
        uses: swatinem/rust-cache@v2
        with:
          workspaces: automa-core
          key: ${{ matrix.target }}

      - name: Run Tests
        run: cargo test --verbose --target ${{ matrix.target }}

      - name: Build Release Binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package Binary (Unix)
        if: runner.os != 'Windows'
        run: |
          cd target/${{ matrix.target }}/release
          tar -czf ../../../${{ matrix.asset_name }} ${{ matrix.binary_name }}
          cd ../../..
          shasum -a 256 ${{ matrix.asset_name }} > ${{ matrix.asset_name }}.sha256

      - name: Package Binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path "target/${{ matrix.target }}/release/${{ matrix.binary_name }}" -DestinationPath "${{ matrix.asset_name }}"
          (Get-FileHash -Algorithm SHA256 "${{ matrix.asset_name }}").Hash.ToLower() + "  ${{ matrix.asset_name }}" | Out-File -Encoding ascii "${{ matrix.asset_name }}.sha256"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: |
            automa-core/${{ matrix.asset_name }}
            automa-core/${{ matrix.asset_name }}.sha256
          retention-days: 5

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Determine Release Tag
        id: tag
        run: |
          if [ -n "${{ github.event.inputs.tag }}" ]; then
            echo "RELEASE_TAG=${{ github.event.inputs.tag }}" >> $GITHUB_OUTPUT
          else
            echo "RELEASE_TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.RELEASE_TAG }}
          files: release-assets/*
          draft: false
          prerelease: false
          generate_release_notes: true
