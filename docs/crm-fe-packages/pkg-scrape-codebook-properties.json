{
  "id": "pkg-scrape-codebook-properties",
  "name": "[scrape] Extract Category Details",
  "description": "Quét các giá trị trong Drawer của Category và lưu vào {{variables.scraped_codebook_properties}}",
  "icon": "riFileListLine",
  "inputs": [
    {
      "id": "in_1",
      "name": "start",
      "blockId": "js_prop_1",
      "handleId": "js_prop_1-input-1"
    }
  ],
  "outputs": [
    {
      "id": "out_1",
      "name": "end",
      "blockId": "js_prop_1",
      "handleId": "js_prop_1-output-1"
    }
  ],
  "settings": {
    "asBlock": true
  },
  "data": {
    "nodes": [
      {
        "id": "js_prop_1",
        "type": "BlockBasic",
        "label": "javascript-code",
        "data": {
          "code": "const handles = document.querySelectorAll('.drag-handle');\nconst data = Array.from(handles).map(h => {\n  const row = h.closest('.group');\n  if(!row) return null;\n  const inputs = row.querySelectorAll('input');\n  const code = inputs[0]?.value || '';\n  const name = inputs[1]?.value || '';\n  return { code, name };\n}).filter(Boolean);\nautomaSetVariable('scraped_codebook_properties', data);\nautomaNextBlock({ properties: data });",
          "timeout": 10000,
          "description": "Extract Properties inside Drawer"
        },
        "position": {
          "x": 200,
          "y": 100
        }
      }
    ],
    "edges": []
  }
}