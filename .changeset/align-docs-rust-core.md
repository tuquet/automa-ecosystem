---
"automa-ecosystem": minor
---

Align documentation with the new Rust Daemon architecture. Updated CLI and VSCode markdown files to reflect the transition from Node.js (automa-cli) to Rust (automa-core). Addressed security flaws in documentation (CORS, IP binding, and secrets passing).
